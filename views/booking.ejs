<!DOCTYPE html>
<html>
  <head>
    <title>Single Bookings</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <table>
        <tr>
            <th>Email</th>
            <th>Number of Tickets</th>
            <th>Hero</th>
        </tr>

            <tr>
                <td><%= booking.email %></td>
                <td><%= booking.numTickets %></td>
                <td><%= booking.superhero %></td>
            </tr>

     </table>


        <form action="/bookings/delete/<%= booking._id %>" method="POST">
            <button type="submit">Delete</button>
        </form>
  </body>
</html>